<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- ОБЯЗАТЕЛЬНЫЕ РАЗРЕШЕНИЯ ДЛЯ ПОКАЗА РЕКЛАМЫ И ИНТЕРНЕТА -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AI_google_15">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.AI_google_15">
            <!--
               ИСПРАВЛЕНО: android:screenOrientation="portrait" заменено на "fullUser".
               Это позволяет игре плавно адаптироваться под любые повороты устройства (как вертикальные, так и альбомные),
               задействуя все преимущества нашей новой GameViewModel.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>